name: Hunyuan3D-2.1
version: "1.0.0"
type: app

dependencies:
  - python@3.11

scripts:
  install: |
    pip install --upgrade pip
    pip install -r requirements.txt
  start: |
    python gradio_app.py

ports:
  - 8080
